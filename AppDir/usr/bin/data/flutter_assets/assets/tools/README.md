Bundled ExifTool executables should be placed in platform folders:

- windows/exiftool.exe
- macos/x64/exiftool
- macos/arm64/exiftool
- android/exiftool
- ios/exiftool

These placeholder files are scaffold-only and must be replaced with real binaries before release.
